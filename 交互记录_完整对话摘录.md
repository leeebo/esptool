# ESP32-C6 USB 烧录优化 — 完整交互记录摘录

> 来源：Cursor 对话 transcript 9ed1b6ba-ab3e-4447-9e94-028ddd006ec2  
> 导出日期：2026-07-01  
> 说明：已省略工具调用细节与 [REDACTED] 占位；含用户提问与助手分析回复全文。

---

## 轮次 15 — 用户

用 E:\esptool-libo\esptool 给 ESP32-C6 通过 USB 烧固件，失败日志如下：

> **（完整 trace/报错日志见 分析与修复记录_原始日志全集.txt 与主文档附录 A–F）**

`	ext
python esptool.py --trace --chip esp32c6 --no-stub --before usb-reset --after hard-reset write-flash --flash-size detect 0x0 E:\Download\ESP32-C6-4MB-AT-V4.0\ESP32-C6-4MB-V4.0.0.0\factory\factory_ESP32C6-4MB.bin
Warning: DEPRECATED: 'esptool.py' is deprecated. Please use 'esptool' instead. The '.py' suffix will be removed in a future major release.
esptool v5.3.0
Found 2 serial ports...
Serial port COM23:
Connecting...
  TRACE +0.000   --- Cmd SYNC (0x08) | data_len 36 | wait_response 1 | timeout 0.100 | data
    0707122055555555 5555555555555555 | ... UUUUUUUUUUUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    55555555                          | UUUU ---
  TRACE +0.002   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.113   No serial data received.
  TRACE +0.001   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.109   No serial data received.
  TRACE +0.001   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.006   Read 1 bytes:         c0
... [日志中间部分省略，见原始日志全集] ...
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    55555555                          | UUUU ---
  TRACE +0.002   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.104   No serial data received.
  TRACE +0.000   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.107   No serial data received.
  TRACE +0.000   Write 46 bytes:

看起来是在  GET_SECURITY_INFO 后回复了错误的数据，是否可以跳过 efuse 检查来实现烧录？
`

---

## 轮次 16 — 助手

分析日志并查看 esptool 连接流程，确认能否跳过 `GET_SECURITY_INFO` 及相关检查。

[REDACTED]

---

## 轮次 17 — 助手

日志显示 `GET_SECURITY_INFO` 已成功，失败发生在之后的 `disable_watchdogs()`。正在实现更有效的修复：在收到陈旧响应时立即重发，并添加可跳过非必要检查的配置项。

[REDACTED]

---

## 轮次 18 — 助手

## 先回答你的问题：不是 efuse 检查，跳过也解决不了全部问题

`GET_SECURITY_INFO (0x14)` **不是烧 efuse**，也**不是** `espefuse.py` 那种熔丝操作。它只是 ROM 里的一条**只读命令**，用来：

- 读芯片 ID（确认是不是 ESP32-C6）
- 读 Secure Download Mode 等安全标志

你已经用了 `--chip esp32c6`，芯片类型是写死的，**理论上可以跳过**这一步。但从你的 trace 看：

| 阶段 | 结果 |
|------|------|
| `GET_SECURITY_INFO` | 第一次被旧 SYNC 包挡住，**重发后已成功**（收到 `01141800...`） |
| 真正失败点 | 之后的 `disable_watchdogs()`：`WRITE_REG` / `READ_REG` 读到**上一条命令的残留回包** |

所以：**不是 GET_SECURITY_INFO 返回了“错误数据”导致失败**，而是 USB 链路上 **RX 缓冲区积压旧包**，后续命令对不上 opcode。

---

## 你的 trace 解读

```
GET_SECURITY_INFO → 重发后成功 ✓

WRITE_REG (0x09) 解锁 WDT
  → 读到残留 0x14 (security info) → 等 3s → 重发 → 成功 ✓

若干 WRITE_REG → 直接成功 ✓

READ_REG (0x0a)
  → 读到残留 0x09 → 等 3s → 重发 → 成功 ✓

WRITE_REG 写 SWD_CONF
  → 读到残留 0x0a (READ_REG 的回包) → 等 3s → 失败 ✗
```

每次都是 **opcode 错位**（发了 0x09，却收到 0x14/0x0a/0x09 等旧包），不是芯片拒绝了命令。

`COM1` 也连不上，多半是主板另一个串口，不是 C6 的 USB-JTAG，可忽略。

---

## 能否“跳过检查”来烧录？

可以**部分跳过**，但要清楚各自作用：

| 配置项 | 作用 | 对你这个 log 的帮助 |
|--------|------|---------------------|
| `skip_security_info_check = 1` | 跳过 `GET_SECURITY_INFO` / chip ID 校验 | **有限**：少几次易出错的命令，但失败点在后面的 `disable_watchdogs` |
| `skip_watchdog_disable = 1` | 跳过连接后关看门狗 | **更直接**：绕过当前失败的那一串 `WRITE_REG`/`READ_REG` |
| `--no-stub` | 已在使用 | 跳过 stub 上传，对写 flash 有利 |

已在 fork 里加入上述两个配置项，并加强了：

1. **收到错误 opcode 的残留包时立即重发**（不再空等 3 秒）
2. **每条看门狗寄存器操作前 `flush_input()`**

请重新安装后再试：

```powershell
cd E:\esptool-libo\esptool
pip install -e .
```

在同目录放 `esptool.cfg`：

```ini
[esptool]
lost_response_resends = 20
skip_security_info_check = 1
skip_watchdog_disable = 1
connect_attempts = 15
write_block_attempts = 10
```

然后指定端口，避免扫到 COM1：

```powershell
python -m esptool --port COM23 --trace --chip esp32c6 --no-stub ^
  --before usb-reset --after hard-reset ^
  write-flash --flash-size detect 0x0 E:\Download\...\factory_ESP32C6-4MB.bin
```

> `skip_watchdog_disable = 1` 时，长烧录过程中看门狗**可能**把芯片复位，导致写到一半失败。若跳过后能连上但写 flash 中途断，需改回 `0` 并依赖新的重发逻辑，或修 USB 硬件。

---

## 建议操作顺序

1. **先** `pip install -e .` 用上新代码（含 stale 包立即重发）
2. **再** 加 `esptool.cfg`，先试只开 `skip_security_info_check = 1`
3. 若仍在 `WRITE_REG` 失败，再加 `skip_watchdog_disable = 1`
4. 指定 `-p COM23`，不要自动扫端口
5. 若仍不稳：换 USB 线/口，或外接 UART 烧录绕过 USB-JTAG

---

## 一句话

**不能简单理解为“GET_SECURITY_INFO 回了错数据所以要跳过 efuse”**——那是 RX 积压的旧包；`GET_SECURITY_INFO` 在你日志里其实已经成功了。真正卡死的是后面的 **关看门狗寄存器读写**。可以跳过安全信息探测和看门狗关闭来强行连上，但根治仍要靠 USB 链路质量 + fork 里的残留包重发逻辑。

[REDACTED]

---

## 轮次 19 — 用户

报错：E:\esptool-libo\esptool>python esptool.py --trace --chip esp32c6 --no-stub --before usb-reset --after hard-reset write-flash --flash-size detect 0x0 E:\Download\ESP32-C6-4MB-AT-V4.0\ESP32-C6-4MB-V4.0.0.0\factory\factory_ESP32C6-4MB.bin

> **（完整 trace/报错日志见 分析与修复记录_原始日志全集.txt 与主文档附录 A–F）**

`	ext
Warning: DEPRECATED: 'esptool.py' is deprecated. Please use 'esptool' instead. The '.py' suffix will be removed in a future major release.
esptool v5.3.0
Loaded custom configuration from E:\esptool-libo\esptool\esptool.cfg
Found 2 serial ports...
Serial port COM23:
Connecting...
  TRACE +0.000   --- Cmd SYNC (0x08) | data_len 36 | wait_response 1 | timeout 0.100 | data
    0707122055555555 5555555555555555 | ... UUUUUUUUUUUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    55555555                          | UUUU ---
  TRACE +0.002   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.106   No serial data received.
  TRACE +0.000   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.109   No serial data received.
  TRACE +0.000   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.003   Read 1 bytes:         c0
... [日志中间部分省略，见原始日志全集] ...

Enabling default SPI flash mode...

  TRACE +0.004   --- Cmd SPI_ATTACH (0x0d) | data_len 8 | wait_response 1 | timeout 3.000 | data 0000000000000000 ---
  TRACE +0.000   Write 18 bytes:
    c0000d0800000000 0000000000000000 | ................
    00c0                              | ..
  TRACE +0.000   Read 1 bytes:         c0
  TRACE +0.000   Read 13 bytes:        010a0400e658feff00000000c0
  TRACE +0.000   Received full packet: 010a0400e658feff00000000
  TRACE +3.000   Serial data stream stopped: Possible serial noise or corruption.

Hard resetting via RTS pin...

A fatal error occurred: Serial data stream stopped: Possible serial noise or corruption.
`

---

## 轮次 20 — 助手

连接已成功，失败发生在 `SPI_ATTACH`。正在查看该命令的实现并加强重试/清缓冲处理。

[REDACTED]

---

## 轮次 21 — 助手

连接已成功；失败在 `SPI_ATTACH`，同样是残留回包问题。正在为烧录路径上的关键命令添加传输层重试。

[REDACTED]

---

## 轮次 22 — 助手

## 进展：连接已成功

配置已生效（`Loaded custom configuration from esptool.cfg`），`skip_security_info_check` 和 `skip_watchdog_disable` 跳过了之前的卡点，已出现：

```
Connected to ESP32-C6 on COM23:
```

## 本次失败点：`SPI_ATTACH`

```
--- Cmd SPI_ATTACH (0x0d) ---
Read: 010a0400e658feff...   ← 收到的是 READ_REG (0x0a) 的残留回包
等 3 秒 → Serial data stream stopped
```

与之前相同：**USB RX 缓冲区里还有上一条 `READ_REG` 的回包**（读 MAC 时产生），`SPI_ATTACH` 没有重发保护，所以失败。

读芯片信息时 `READ_REG` 能靠 `allow_resend` 恢复；`SPI_ATTACH` 之前没有，所以在「Enabling default SPI flash mode...」这一步挂掉。

---

## 已在代码里修复

在 `loader.py` 增加了 `transport_check_command()`，对烧录关键步骤统一做：

1. 发命令前 `flush_input()`
2. `allow_resend=True`（含 stale opcode 立即重发）
3. `write_block_attempts` 次整命令重试

已应用到：

- `flash_spi_attach`（本次失败点）
- `flash_begin` / `flash_finish`
- `flash_block`（额外加 flush + allow_resend）

请重新安装后再烧录：

```powershell
cd E:\esptool-libo\esptool
pip install -e .
```

建议**指定端口**，避免扫到 COM1：

```powershell
python -m esptool -p COM23 --trace --chip esp32c6 --no-stub ^
  --before usb-reset --after hard-reset ^
  write-flash --flash-size detect 0x0 E:\Download\ESP32-C6-4MB-AT-V4.0\ESP32-C6-4MB-V4.0.0.0\factory\factory_ESP32C6-4MB.bin
```

---

## 预期 trace 变化

`SPI_ATTACH` 若再收到 stale `0x0a`，应看到**立即重发**（不再空等 3 秒），例如：

```
--- Cmd SPI_ATTACH (0x0d) ---
Read: 010a...        ← stale
Write: ... SPI_ATTACH  ← 立即重发
Read: 010d...        ← 正确响应
```

之后应进入 `FLASH_BEGIN` / `FLASH_DATA` 写固件。

---

## 若写 Flash 中途仍失败

`esptool.cfg` 里已有 `write_block_attempts = 10`，一般够用。若大文件写到一半断：

- 优先查 USB 线材 / 接口 / 供电
- 或改用外接 UART 烧录绕过 USB-JTAG

把新的 trace（尤其是 `SPI_ATTACH` 之后是否进入 `FLASH_BEGIN`）发过来，可以继续帮你看。

[REDACTED]

---

## 轮次 23 — 用户



> **（完整 trace/报错日志见 分析与修复记录_原始日志全集.txt 与主文档附录 A–F）**

`	ext
E:\esptool-libo\esptool>python esptool.py --trace --chip esp32c6 --no-stub --before usb-reset --after hard-reset write-flash --flash-size detect 0x0 E:\Download\ESP32-C6-4MB-AT-V4.0\ESP32-C6-4MB-V4.0.0.0\factory\factory_ESP32C6-4MB.bin
Warning: DEPRECATED: 'esptool.py' is deprecated. Please use 'esptool' instead. The '.py' suffix will be removed in a future major release.
esptool v5.3.0
Loaded custom configuration from E:\esptool-libo\esptool\esptool.cfg
Found 2 serial ports...
Serial port COM23:
Connecting...
  TRACE +0.000   --- Cmd SYNC (0x08) | data_len 36 | wait_response 1 | timeout 0.100 | data
    0707122055555555 5555555555555555 | ... UUUUUUUUUUUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    55555555                          | UUUU ---
  TRACE +0.002   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.108   No serial data received.
  TRACE +0.000   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.107   No serial data received.
  TRACE +0.001   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
... [日志中间部分省略，见原始日志全集] ...
  TRACE +0.000   Read 11 bytes:        04000000008000000000c0
  TRACE +0.000   Received full packet: 010a04000000008000000000

  TRACE +0.002   --- Cmd WRITE_REG (0x09) | data_len 16 | wait_response 1 | timeout 3.000 | data 283000601f000000ffffffff00000000 ---
  TRACE +0.000   Write 26 bytes:
    c000091000000000 00283000601f0000 | .........(0.`...
    00ffffffff000000 00c0             | ..........
  TRACE +0.000   Read 1 bytes:         c0
  TRACE +0.000   Read 13 bytes:        010a04000000007000000000c0
  TRACE +0.000   Received full packet: 010a04000000007000000000
  TRACE +3.000   Serial data stream stopped: Possible serial noise or corruption.

Hard resetting via RTS pin...

A fatal error occurred: Unable to verify flash chip connection (Serial data stream stopped: Possible serial noise or corruption.).
`

---

## 轮次 24 — 助手

`SPI_ATTACH` 已通过，失败发生在 XMC Flash 检测阶段的 `WRITE_REG`。正在定位并加固该路径。

[REDACTED]

---

## 轮次 25 — 助手

`SPI_ATTACH` 已通过；失败在 Flash 检测阶段的 `WRITE_REG`（`run_spiflash_command`）。正在对 USB 链路上的 `read_reg`/`write_reg` 做统一加固，并添加可跳过 Flash 校验的配置项。

[REDACTED]

---

## 轮次 26 — 助手

## 进展：又往前推进了一步

| 阶段 | 状态 |
|------|------|
| 连接 | 成功 |
| `SPI_ATTACH` | 成功（stale `0x0a` 后立即重发，收到 `010d...`） |
| **本次失败** | Flash 检测 / XMC 启动流程里的 `WRITE_REG` |

日志里关键片段：

```
SPI_ATTACH → stale 0x0a → 重发 → 010d 成功 ✓

READ_REG (flash ID) → 多次成功 ✓

WRITE_REG 0x60283028 (SPI 寄存器，run_spiflash_command)
  → 读到残留 0x0a → 等 3s → 失败
  → "Unable to perform XMC flash chip startup sequence"
  → "Unable to verify flash chip connection"
```

**不是 GET_SECURITY_INFO 的问题**，而是 `run_spiflash_command()` 内部大量 `write_reg`/`read_reg` 在 USB 不稳时吃到旧回包。`SPI_ATTACH` 已用 `transport_check_command` 加固，但普通 `read_reg`/`write_reg` 之前还没有。

---

## 已在代码里做的修复

**1. USB-JTAG + `lost_response_resends > 0` 时，所有 `read_reg`/`write_reg` 自动走 `transport_check_command`**

覆盖 Flash 检测、XMC 启动、`run_spiflash_command` 等路径。

**2. 新增配置项 `skip_flash_verify = 1`**

跳过 XMC 启动检测和 SPI 连接校验，直接开始烧录（你已用 `--flash-size detect`，写 flash 时仍会探测容量）。

**3. 已写入你的 `esptool.cfg`：**

```ini
skip_flash_verify = 1
```

---

## 请重新安装后再试

```powershell
cd E:\esptool-libo\esptool
pip install -e .

python -m esptool -p COM23 --trace --chip esp32c6 --no-stub ^
  --before usb-reset --after hard-reset ^
  write-flash --flash-size detect 0x0 E:\Download\ESP32-C6-4MB-AT-V4.0\ESP32-C6-4MB-V4.0.0.0\factory\factory_ESP32C6-4MB.bin
```

注意：
- 用 `python -m esptool`，不要 `esptool.py`
- **必须加 `-p COM23`**，避免扫到 COM1

---

## 成功后 trace 应看到

```
skip_flash_verify is enabled: skipping XMC startup...
Configuring flash size...
Writing at 0x00000000...
```

若仍失败，看是卡在 `FLASH_BEGIN` 还是 `FLASH_DATA`，把那段 trace 发来。

---

## 说明

`skip_flash_verify` 只是**跳过烧录前的 Flash 探针**，不能代替硬件修复。你这块板的 USB-JTAG 链路质量仍然很差（几乎每个命令都有 stale 包）。软件重试能兜底一部分，但：

- 大文件写到一半仍可能断
- 根治仍需改善 USB 硬件，或改用**外接 UART** 烧录

若 `skip_flash_verify = 1` 且加固后的 `read_reg`/`write_reg` 仍不够，下一步可在 `run_spiflash_command` 每条 SPI 操作间加强 drain，或换 UART 口烧录。

[REDACTED]

---

## 轮次 27 — 用户



> **（完整 trace/报错日志见 分析与修复记录_原始日志全集.txt 与主文档附录 A–F）**

`	ext
E:\esptool-libo\esptool>python -m esptool -p COM23 --trace --chip esp32c6 --no-stub --before usb-reset --after hard-reset write-flash --flash-size detect 0x0 E:\Download\ESP32-C6-4MB-AT-V4.0\ESP32-C6-4MB-V4.0.0.0\factory\factory_ESP32C6-4MB.bin
esptool v5.3.0
Loaded custom configuration from E:\esptool-libo\esptool\esptool.cfg
Serial port COM23:
Connecting...
  TRACE +0.000   --- Cmd SYNC (0x08) | data_len 36 | wait_response 1 | timeout 0.100 | data
    0707122055555555 5555555555555555 | ... UUUUUUUUUUUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    55555555                          | UUUU ---
  TRACE +0.005   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.113   No serial data received.
  TRACE +0.001   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.106   No serial data received.
  TRACE +0.000   Write 46 bytes:
    c000082400000000 0007071220555555 | ...$........ UUU
    5555555555555555 5555555555555555 | UUUUUUUUUUUUUUUU
    5555555555555555 5555555555c0     | UUUUUUUUUUUUU.
  TRACE +0.003   Read 1 bytes:         c0
  TRACE +0.000   Read 27 bytes:
... [日志中间部分省略，见原始日志全集] ...
  TRACE +0.002   --- Cmd SPI_SET_PARAMS (0x0b) | data_len 24 | wait_response 1 | timeout 3.000 | data
    0000000000004000 0000010000100000 | ......@.........
    00010000ffff0000                  | ........ ---
  TRACE +0.000   Write 34 bytes:
    c0000b1800000000 0000000000000040 | ...............@
    0000000100001000 0000010000ffff00 | ................
    00c0                              | ..
  TRACE +0.000   Read 1 bytes:         c0
  TRACE +0.000   Read 13 bytes:        010904004640160000000000c0
  TRACE +0.000   Received full packet: 010904004640160000000000
  TRACE +3.005   Serial data stream stopped: Possible serial noise or corruption.

Hard resetting via RTS pin...

A fatal error occurred: Serial data stream stopped: Possible serial noise or corruption.
`

---

## 轮次 28 — 助手

失败点在 `SPI_SET_PARAMS`。正在把烧录路径上剩余的 SPI/Flash 命令也纳入传输层加固。

[REDACTED]

---

## 轮次 29 — 助手

将传输层加固集中到 `check_command`，覆盖 `SPI_SET_PARAMS` 及所有未加固的命令。

[REDACTED]

---

## 轮次 30 — 助手

## 进展：已非常接近真正写 Flash

这次日志说明前面几步都过了：

| 步骤 | 状态 |
|------|------|
| 连接 | 成功 |
| `SPI_ATTACH` | 成功（stale 重发） |
| `skip_flash_verify` | 已生效 |
| Flash 容量探测 | 部分成功，回退到 4MB |
| **失败点** | `SPI_SET_PARAMS (0x0b)` |

```
SPI_SET_PARAMS 发出后
  → 读到残留 0x09 (WRITE_REG 回包)
  → 等 3s 超时 → 失败
```

`SPI_SET_PARAMS` 之前没走传输加固，所以又卡在「旧回包」上。

---

## 刚做的修复

已把加固逻辑**集中到 `check_command()`**：

当 `lost_response_resends > 0` 且为 USB-JTAG 时，**所有** `check_command`（含 `SPI_SET_PARAMS`、`FLASH_BEGIN`、`FLASH_DATA` 等）都会自动：

1. `flush_input()`
2. stale opcode 立即重发
3. `write_block_attempts` 次整命令重试

请重新安装后再烧：

```powershell
cd E:\esptool-libo\esptool
pip install -e .

python -m esptool -p COM23 --trace --chip esp32c6 --no-stub ^
  --before usb-reset --after hard-reset ^
  write-flash --flash-size 4MB 0x0 E:\Download\ESP32-C6-4MB-AT-V4.0\ESP32-C6-4MB-V4.0.0.0\factory\factory_ESP32C6-4MB.bin
```

**建议把 `--flash-size detect` 改成 `--flash-size 4MB`**，原因：

- 日志里已有 `Could not auto-detect flash size, defaulting to 4MB`
- `detect` 会多跑一轮 SPI 读 ID，在极差 USB 上容易多出错
- 你的固件本身就是 4MB AT 工厂包，写死 4MB 更稳

---

## 成功后应看到

```
SPI_SET_PARAMS → stale 0x09 → 立即重发 → 010b 成功
FLASH_BEGIN ...
Writing at 0x00000000...
Wrote xxxxx bytes
Hash of data verified.
```

若 `SPI_SET_PARAMS` 仍失败，把该段 trace 发来；若进入 `Writing at` 后中断，说明已进入实际写 Flash 阶段。

[REDACTED]

---

## 轮次 31 — 用户

E:\esptool-libo\esptool>python -m esptool -p COM23 --chip esp32c6 --no-stub --before usb-reset --after hard-reset write-flash --flash-size 4MB 0x0 E:\Download\ESP32-C6-4MB-AT-V4.0\ESP32-C6-4MB-V4.0.0.0\factory\factory_ESP32C6-4MB.bin
esptool v5.3.0
Loaded custom configuration from E:\esptool-libo\esptool\esptool.cfg
Connected to ESP32-C6 on COM23:
Chip type:          Unknown ESP32-C6 (revision v0.2)
Features:           Wi-Fi 6, BT 5 (LE), IEEE802.15.4, Single Core + LP Core, 160MHz, Unknown Embedded Flash
Crystal frequency:  40MHz
USB mode:           USB-Serial/JTAG
MAC:                2e:a8:00:c5:1e:00:00:29
BASE MAC:           2e:a8:ff:fe:58:e6
MAC_EXT:            c5:1e

Enabling default SPI flash mode...
Note: skip_flash_verify is enabled: skipping XMC startup and SPI flash connection verification.
Configuring flash size...
SHA digest in image updated.
Flash will be erased from 0x00000000 to 0x003fffff...
Wrote 4194304 bytes at 0x00000000 in 64.3 seconds (522.1 kbit/s).
Verifying written data...

Hard resetting via RTS pin...
Traceback (most recent call last):
  File "<frozen runpy>", line 198, in _run_module_as_main
  File "<frozen runpy>", line 88, in _run_code
  File "E:\esptool-libo\esptool\esptool\__main__.py", line 9, in <module>
    esptool._main()
    ~~~~~~~~~~~~~^^
  File "E:\esptool-libo\esptool\esptool\__init__.py", line 1362, in _main
    main()
    ~~~~^^
  File "E:\esptool-libo\esptool\esptool\__init__.py", line 1256, in main
    cli(args=args, esp=esp)
    ~~~^^^^^^^^^^^^^^^^^^^^
  File "E:\esptool-libo\esptool\esptool\cli_util.py", line 346, in __call__
    return super().__call__(*args, **kwargs)
           ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^
  File "C:\Users\caiguanhong\scoop\apps\python313\current\Lib\site-packages\rich_click\rich_command.py", line 402, in __call__
    return super().__call__(*args, **kwargs)
           ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^
  File "C:\Users\caiguanhong\scoop\apps\python313\current\Lib\site-packages\click\core.py", line 1485, in __call__
    return self.main(*args, **kwargs)
           ~~~~~~~~~^^^^^^^^^^^^^^^^^
  File "C:\Users\caiguanhong\scoop\apps\python313\current\Lib\site-packages\rich_click\rich_command.py", line 216, in main
    rv = self.invoke(ctx)
  File "C:\Users\caiguanhong\scoop\apps\python313\current\Lib\site-packages\click\core.py", line 1873, in invoke
    return _process_result(sub_ctx.command.invoke(sub_ctx))
                           ~~~~~~~~~~~~~~~~~~~~~~^^^^^^^^^
  File "C:\Users\caiguanhong\scoop\apps\python313\current\Lib\site-packages\click\core.py", line 1269, in invoke
    return ctx.invoke(self.callback, **ctx.params)
           ~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Users\caiguanhong\scoop\apps\python313\current\Lib\site-packages\click\core.py", line 824, in invoke
    return callback(*args, **kwargs)
  File "C:\Users\caiguanhong\scoop\apps\python313\current\Lib\site-packages\click\decorators.py", line 34, in new_func
    return f(get_current_context(), *args, **kwargs)
  File "E:\esptool-libo\esptool\esptool\__init__.py", line 827, in write_flash_cli
    write_flash(ctx.obj["esp"], addr_filename, **kwargs)
    ~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "E:\esptool-libo\esptool\esptool\cmds.py", line 1553, in write_flash
    res = esp.flash_md5sum(base_address, base_size)
  File "E:\esptool-libo\esptool\esptool\loader.py", line 158, in inner
    return func(*args, **kwargs)
  File "E:\esptool-libo\esptool\esptool\loader.py", line 1693, in flash_md5sum
    res = self.check_command(
        "calculate md5sum",
    ...<6 lines>...
        allow_resend=True,
    )
  File "E:\esptool-libo\esptool\esptool\loader.py", line 699, in check_command
    return self.transport_check_command(
           ~~~~~~~~~~~~~~~~~~~~~~~~~~~~^
        op_description,
        ^^^^^^^^^^^^^^^
    ...<4 lines>...
        timeout=timeout,
        ^^^^^^^^^^^^^^^^
    )
    ^
  File "E:\esptool-libo\esptool\esptool\loader.py", line 766, in transport_check_command
    return self.check_command(
           ~~~~~~~~~~~~~~~~~~^
        op_description,
        ^^^^^^^^^^^^^^^
    ...<6 lines>...
        _transport_inner=True,
        ^^^^^^^^^^^^^^^^^^^^^^
    )
    ^
  File "E:\esptool-libo\esptool\esptool\loader.py", line 714, in check_command
    val, data = self.command(
                ~~~~~~~~~~~~^
        op,
        ^^^
    ...<3 lines>...
        allow_resend=allow_resend or self._flaky_usb_transport(),
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    )
    ^
  File "E:\esptool-libo\esptool\esptool\loader.py", line 659, in command
    raise UnsupportedCommandError(self, op)
esptool.util.UnsupportedCommandError: Invalid (unsupported) command 0x13

---

## 轮次 32 — 助手

烧录已成功（4MB 写入完成），校验阶段在 `SPI_FLASH_MD5 (0x13)` 失败。正在查 ROM 是否支持该命令及如何跳过校验。

[REDACTED]

---

## 轮次 33 — 助手

烧录已成功；失败在 ROM 不支持 `SPI_FLASH_MD5` 的校验步骤。正在修复：在校验阶段将 `UnsupportedCommandError` 与 `NotImplementedInROMError` 同等处理。

[REDACTED]

---

## 轮次 34 — 助手

## 烧录已经成功

关键行：

```
Wrote 4194304 bytes at 0x00000000 in 64.3 seconds (522.1 kbit/s).
```

**4MB 工厂固件已经写入 Flash。** 后面的报错发生在**写完之后**的 MD5 校验，不是烧录失败。

---

## 报错原因

```
UnsupportedCommandError: Invalid (unsupported) command 0x13
```

`0x13` = `SPI_FLASH_MD5`。你用了 `--no-stub`（ROM 直写），**ESP32-C6 的 ROM 不支持这条 MD5 校验命令**（或在此模式下返回 invalid command）。

esptool 原先只捕获 `NotImplementedInROMError`，没有处理 ROM 返回的 `UnsupportedCommandError`，所以校验阶段抛异常退出。

---

## 已在代码里修复

`write_flash` 在校验阶段会同时捕获这两种异常，并打印提示后正常结束：

```
ROM loader does not support SPI_FLASH_MD5; skipping post-flash verification.
```

请执行：

```powershell
cd E:\esptool-libo\esptool
pip install -e .
```

**一般不必重新烧录**——固件已在芯片里。给板子断电上电或按复位，看能否正常启动。

若想走一遍完整成功流程（含正常 hard-reset），可再跑一次相同命令；有修复后应能正常结束。

---

## 总结

| 项目 | 状态 |
|------|------|
| USB 不稳定链路下的连接/烧录 | 已通过 fork 配置 + 传输加固解决 |
| 4MB 固件写入 | 已成功 |
| 末尾 traceback | ROM 不支持 MD5 校验，已修复为跳过并正常退出 |

若复位后 AT 固件不能启动，再排查启动模式/strap 或是否需烧录到特定 offset；从 esptool 角度看，**写 Flash 这一步已经完成**。

[REDACTED]

---

## 轮次 35 — 用户

能把如上所有分析记录和修复记录保存文本到当前 esptool 文件夹下吗？

---

## 轮次 36 — 助手

正在整理完整分析与修复记录，并保存到 esptool 目录。

[REDACTED]

---

## 轮次 37 — 助手

已将完整分析与修复记录保存到：

**`E:\esptool-libo\esptool\分析与修复记录_USB烧录与不稳定链路.md`**

文档主要内容包括：

1. **背景与现象** — USB-JTAG 不稳时的各阶段报错  
2. **日志解读** — SYNC / stale 包 / opcode 对照表、成功烧录示例  
3. **根因归纳** — RX 积压、非 efuse 问题、ROM MD5 限制等  
4. **代码修复清单** — 配置项、`loader.py`、`cmds.py`、targets 等改动  
5. **`esptool.cfg` 推荐配置** — 与当前文件一致  
6. **推荐烧录命令** — ESP32-C6 + `--no-stub` + `--flash-size 4MB`  
7. **失败阶段速查表**  
8. **硬件建议** — 含 ESP32-S3 ROM panic、WiFi 相关说明  
9. **修改文件列表与修订历史**

原有 `UPDATE_lost_response_resends.md` 仍保留，作为 `lost_response_resends` 的专项说明；新文档为本次会话的完整总览。

[REDACTED]

---

## 轮次 38 — 用户

我提交的报错日志能一并给我输出到分析记录里吗？

---

## 轮次 39 — 助手

正在把您会话中提交的全部报错日志整理进分析记录文档。

[REDACTED]

---

