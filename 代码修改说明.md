# esptool USB 烧录加固 — 代码修改说明

> 基准：`E:\esptool-libo\esptool`（v5.3.0 fork）  
> 补丁文件：[代码修改_diff.patch](代码修改_diff.patch)

---

## 1. `esptool.cfg`（新增）

```ini
[esptool]
lost_response_resends = 20
skip_security_info_check = 1
skip_watchdog_disable = 1
skip_flash_verify = 1
connect_attempts = 15
write_block_attempts = 10
```

从项目根目录自动加载（`Loaded custom configuration from ...`）。

---

## 2. `esptool/config.py`

在 `CONFIG_OPTIONS` 中注册：

- `skip_security_info_check`
- `skip_watchdog_disable`
- `skip_flash_verify`

（`lost_response_resends`、`connect_attempts`、`write_block_attempts` 为既有项，本 fork 通过 cfg 调大。）

---

## 3. `esptool/loader.py`（核心）

### 3.1 配置常量

```python
LOST_RESPONSE_RESENDS = cfg.getint("lost_response_resends", 15)
SKIP_SECURITY_INFO_CHECK = cfg.getint("skip_security_info_check", 0)
SKIP_WATCHDOG_DISABLE = cfg.getint("skip_watchdog_disable", 0)
SKIP_FLASH_VERIFY = cfg.getint("skip_flash_verify", 0)
```

### 3.2 `command()` — 两类重发

**A. 响应完全丢失**（原有逻辑，`SerialReaderStoppedError`）  
**B. 陈旧 opcode**（新增）：收到合法 SLIP 包但 `op_ret != op` 时，若 `allow_resend` 且有余量，**立即** `flush_input()` + 重发，不再空等满 3s 超时。

```661:669:esptool/loader.py
                # Stale reply in the RX backlog (valid packet, wrong opcode).
                if allow_resend and resends_left > 0:
                    resends_left -= 1
                    self.flush_input()
                    self.write(pkt)
                    continue
```

### 3.3 `_drain_stale_input(idle_timeout=0.05)`

`sync()` 成功后调用：短超时循环 `read()`，丢弃已在 RX 中的多余 SLIP 包（典型为多个 SYNC 0x08 回显）。

### 3.4 `_flaky_usb_transport()`

```python
return LOST_RESPONSE_RESENDS > 0 and self.uses_usb_jtag_serial()
```

为真时，传输层走加固路径。

### 3.5 `check_command()`

- flaky USB 且非内部调用 → 委托 `transport_check_command()`
- 否则 `command(..., allow_resend=allow_resend or self._flaky_usb_transport())`

### 3.6 `transport_check_command()`

外层循环 `WRITE_BLOCK_ATTEMPTS` 次：

1. `flush_input()`
2. `check_command(..., allow_resend=True, _transport_inner=True)`
3. 任意 `FatalError` 则重试直至耗尽

用于 `SPI_ATTACH`、`SPI_SET_PARAMS`、`READ_REG`/`WRITE_REG`（经 `check_command`）等烧录路径命令。

### 3.7 连接阶段

- `SKIP_SECURITY_INFO_CHECK`：跳过 `GET_SECURITY_INFO` / chip ID 校验（信任 `--chip`）
- `SKIP_WATCHDOG_DISABLE`：`_post_connect` 不调用 `disable_watchdogs()`

### 3.8 `read_reg` / `write_reg`

在 flaky USB 上通过已加固的 `check_command` 发送，并默认 `allow_resend`。

---

## 4. `esptool/targets/esp32c3.py`（C6 继承）

`disable_watchdogs()`：

- `SKIP_WATCHDOG_DISABLE` 或非 USB-JTAG → 直接 return
- 否则每条寄存器写前 `flush_input()`，经 `write_reg(..., allow_resend=True)`
- `READ_REG` 读 SWD 配置前也 `flush_input()`

**同步修改**：`esp32s3.py`、`esp32p4.py`（相同 USB 看门狗逻辑）。

---

## 5. `esptool/cmds.py`

### 5.1 `attach_flash()`

```python
if SKIP_FLASH_VERIFY:
    log.note("skip_flash_verify is enabled: skipping XMC startup and SPI flash connection verification.")
    return
```

跳过 XMC 启动序列与 SPI 连接验证（减少 flaky 链路上的额外 SPI 交互）。

### 5.2 `write_flash()` — 写后 MD5

`--no-stub` 时 ROM 可能不支持 `SPI_FLASH_MD5 (0x13)`。除 `NotImplementedInROMError` 外，同时捕获 `UnsupportedCommandError`，记录 note 后视为校验跳过（**写入本身可能已成功**）。

---

## 6. `test/test_lost_response_resends.py`

新增/扩展单元测试：

- 陈旧 opcode 触发重发
- `SerialReaderStoppedError` 窄异常类型
- fake serial port 不依赖硬件

---

## 7. 删除

- `UPDATE_lost_response_resends.md`（内容已合并入本归档）

---

## 8. 调用关系（简图）

```
write-flash / connect
    └─ attach_flash()  [SKIP_FLASH_VERIFY 可提前 return]
    └─ check_command()  [flaky USB → transport_check_command]
           └─ command(allow_resend=True)
                  ├─ SerialReaderStoppedError → flush + resend
                  └─ wrong op_ret → flush + resend (stale)
    └─ _post_connect → disable_watchdogs()  [SKIP_WATCHDOG_DISABLE 可跳过]
    └─ sync() → _drain_stale_input()
```

---

## 9. 未改动的已知边界

- `FLASH_DATA` 等大块写路径仍以 `write_block_attempts` 为主，未对每字节流做无限重发
- stub 上传（`MEM_BEGIN`/`MEM_DATA`）在 `--no-stub` 时不走
- 硬件极差时，软件重试有上限，无法 100% 保证成功
